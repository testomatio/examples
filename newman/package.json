{
  "name": "Example project for tesomatio-reporter using newman",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx newman run collection.json -e env.json -r testomatio",
    "test:fail": "npx newman run collection_fail.json -e env.json -r testomatio"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "newman": "^5.3.2",
    "newman-reporter-testomatio": "^0.1.0"
  }
}
