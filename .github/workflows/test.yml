name: test
on:
  pull_request

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo $GITHUB_REF
      - run: echo $GITHUB_REPOSITORY
      # - run: TESTOMATIO=n7seb4fd0zg8 echo $GITHUB_REPOSITORY
