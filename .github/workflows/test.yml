name: test
on: pull_request

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo $GITHUB_REF
      - run: echo $GITHUB_REPOSITORY
      - run: echo ${{ github.workflow }}
      - run: TESTOMATIO=yapff28430mu TESTOMATIO_URL=https://beta.testomat.io GH_PAT=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 GITHUB_REF=refs/pull/36/merge GITHUB_REPOSITORY=testomatio/examples npm run test animal-sort/
